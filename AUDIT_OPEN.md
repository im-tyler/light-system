# Open audit items

Register LS-01 through LS-14 (scout pass 1, 2026-09-11) is fully fixed and verified - see git history for the `audit LS-NN` commits. Register LS-15 through LS-20 (scout pass 2, 2026-09-11) is likewise fully fixed and verified. Register LS-21 through LS-34 (scout pass 3, 2026-09-12) is fixed; residual verification scope below. Register LS-35 through LS-42 (scout pass 4, 2026-09-12) is fixed except LS-24 (owner decision); notes below. Register LS-43 through LS-51 plus the reopened LS-23 (scout pass 5, 2026-09-12) is fixed; notes below.

## Open decisions for the owner

- **LS-24 (contract decision, no behavior change): cross-toolchain byte identity of generated normals is unpinned.** The builder computes normals with host `sqrt`/`acos` and the prototype CMake does not pin FP contraction (`-ffp-contract`), so the same source asset can produce different normal bytes under different compilers/FP environments, and a content-addressed cache keyed on those bytes is not portable across toolchains. Options: (a) pin FP semantics for the builder (contraction off, explicit math where practical) and quantize normals at serialization so byte identity is a contract; or (b) keep generation unpinned and make cross-platform cache tests semantic (compare decoded values with tolerance) instead of byte-exact. Owner to rule; nothing committed either way. The schema-version pin check from LS-25 (`tools/check_schema_version.sh`, CI) is in.

## Fixed in scout pass 4 (LS-35..LS-42, LS-24 excepted)

- **LS-35 (glTF mirrored-instance winding): fixed.** The importer computes the world-transform determinant per node and reverses triangle winding (swap b/c) for indexed and non-indexed primitives when it is negative, before clustering; shader untouched. Verified with identity vs. `-1`-scale node fixtures (indexed and non-indexed): `0,1,2` vs `0,2,1`. Positive-determinant assets produce byte-identical winding to before.
- **LS-36 (residency bootstrap modes): fixed.** `create_residency_model` takes a `ResidencyBootstrapMode` (default `all` preserves the viewer runtime) and establishes exactly the advertised set with the complement unloaded; `none` asserts zero resident pages; the mode-string parsing is centralized in `runtime_model` and `replay_script` delegates to it. Behavior change: `meridian_replay`/`meridian_residency` frame-0 output for `none`/`base-only`/`lod-only` now reflects those sets (previously all three behaved like `all`). CI checks exit codes only; committed replays still pass. `benchmarks/scripts/run_import_validation.py` re-runs replays but compares no frame-0 counts.
- **LS-37 (swapchain caps intersection): fixed.** Usage intersects `supportedUsageFlags` (TRANSFER_SRC requested only when supported; recorded in `SwapchainContext.images_support_transfer_src`), composite alpha prefers OPAQUE then falls back through supported modes. The screenshot path fails explicitly at request time (stderr + no capture) when TRANSFER_SRC is unavailable, not at startup.
- **LS-38 (screenshot byte order): fixed.** Readback swizzle is driven by `surface_format.format` (BGRA8/RGBA8, UNORM or SRGB); other formats are rejected at capture time with an explicit error instead of writing corrupted output.
- **LS-39 (instance extension availability): fixed.** `VK_KHR_get_physical_device_properties2` is no longer requested (core since 1.1, requested apiVersion 1.2); `VK_KHR_portability_enumeration` and its instance-create flag are enabled only when the loader advertises the extension.
- **LS-40 (texture upload one-shot checks): fixed.** `vkAllocateCommandBuffers`/`vkBeginCommandBuffer`/`vkEndCommandBuffer` are checked with the LS-21 unwind pattern (pool + staging destroyed, error propagated).
- **LS-41 (timestamp wrap): fixed.** The profiler records the graphics queue family's `timestampValidBits` at creation and computes deltas modulo 2^bits (identity at 64); wrap math pinned by static_asserts (no runtime profiler test surface exists; compile-time checks follow the gpu_abi.h convention).
- **LS-42 (bootstrap exit code): fixed.** `meridian_vk_bootstrap` returns 4 with a stderr diagnostic on initialization/submission/fixed-count present-loop failure; completed fixed-frame runs and clean interactive closes return 0. `benchmarks/godot/run_bench.sh` (exit-code-sensitive via `set -e`) expects success on healthy runs and is unaffected.

## Fixed in scout pass 5 (LS-43..LS-51, LS-23 reopened; 2026-09-12)

- **LS-43 (screenshot unsupported-format skip): fixed.** An unsupported surface format is now an explicit skip branch: no semaphore/fence/acquire/copy call runs (the LS-38 fix's `screenshot_path.clear()` used to default `image_acquired` to true and call `vkWaitForFences` on VK_NULL_HANDLE). Failures are recorded in a new `report.capture_status` (printed as `capture_status=` by `meridian_vk_bootstrap`); the run still exits cleanly per the LS-42 verdict.
- **LS-44 (independentBlend): fixed.** Without `independentBlend` (mirrored, not required) both color attachments use the RGBA write mask — mask bits for channels the R32G32_UINT visibility format lacks are ignored, so both visibility words still land (VUID 00605 satisfied). Per-attachment masks (RGBA + RG) are kept when the feature exists.
- **LS-45 (light direction convention): fixed.** Standardized on `light_dir` = surface-to-light direction (the shader's L). The cascade shadow eye moved from `center - light*radius` (below the scene, opposite the light) to `center + light*radius` looking at the center; shader L and the ortho fit are unchanged (the flip mirrors view forward only — the cascade sphere still spans view z in `[-2r, 0]` with the near plane pushed `caster_extent` past the eye toward the light). MoltenVK screenshot compare (gltf_block scene, before/after binaries): 9.2% of pixels changed, mean luminance up — shadow regions moved to the light-opposite side as expected.
- **LS-46 (interpolated normal): fixed.** `main_geometry.frag` normalizes the interpolated (and front-face-signed) normal once; diffuse `ndotl`, the slope-scaled shadow bias, and the hemisphere ambient all consume the unit-length N. Included in the LS-45 screenshot diff.
- **LS-47 (cluster triangle limit vs visibility ABI): fixed.** Manifest validation rejects `cluster_triangle_limit > 256` (visibility stores `local_triangle` in 8 bits; larger limits alias ids). Boundary 256 and all committed manifests (max 124) unaffected; widening the format stays out of scope.
- **LS-48 (sparse glTF index accessors): fixed.** Sparse index accessors decode per-element via `cgltf_accessor_read_index` (sparse-aware in the pinned cgltf; `cgltf_accessor_unpack_indices` returns 0 for sparse). Dense paths unchanged. `generate_sparse_gltf_block.py` now makes the roof INDEX accessor sparse (no-op overrides exercising both read branches); the rebuilt fixture's .vgeo + sidecar hash byte-identical to the dense-index build.
- **LS-23 reopened (fingerprint metadata coverage): fixed.** `compute_content_fingerprint` now folds in the full header/summary metadata the sidecar mirrors (asset_id, source_asset, has_fallback, source counts, textured flag, texture dims, bounds, all totals) on top of payload + page layout. **Fail-closed consequence: fingerprints changed; .vgeo + .summary.txt pairs persisted before 2026-09-12 fail the pairing check on import and must be regenerated (rebuild the asset).** Schema version stays 6; the documented fingerprint definition in `schemas/VGEO_RESOURCE_SCHEMA.md` is updated. Verified: a metadata-only rebuild (asset_id rename) changes the fingerprint.
- **LS-49 (replay script parsing): fixed.** `frame_count`/`resident_budget`/`eviction_grace_frames` parse via `from_chars` full-string + explicit u32 range; `error_thresholds` via `strtof` full-string + `isfinite` (nan/inf rejected). Errors name the key and value. Verified: `4294967296`, `3x`, `nan`, `0.5junk` all rejected; committed replays parse unchanged.
- **LS-50 (fixed char field truncation): fixed.** Manifest validation enforces asset_id <= 63, source_asset <= 255, material names <= 63 characters (limits derived from the disk struct sizes); the serializer truncation stays as defense-in-depth. Committed manifests unaffected.
- **LS-51 (screenshot one-shot checks): fixed.** `vkResetCommandPool`/`vkBegin`/`vkEnd`/`vkQueueSubmit`/`vkQueueWaitIdle` (plus the acquire-fence wait, readback allocation, map, and file open) are checked; on failure no file is published and `capture_status` records a non-success. Verified live on MoltenVK: healthy capture reports `capture ok` and exits 0.

## Residual verification scope (not known defects)

- **Per-frame occlusion paths are compile-verified only.** The drawIndirectCount-path findings (LS-03/05/13 fixes, temporal HZB dispatch) cannot execute on MoltenVK (per-frame refine is epilogue-only there). First run on a non-MoltenVK device should confirm: HZB edge coverage, footprint-max queries, temporal invalidation dispatch, survivor order stability.
- **LS-26/28/32 gates ran on MoltenVK only so far.** The order-preserving run fold, the drawIndirectFirstInstance/multiDrawIndirect/maxDrawIndirectCount gating, and the Vulkan-1.2 `shaderOutputLayer` feature route (vs the EXT extension) all compiled and ran clean on MoltenVK; a non-MoltenVK device should confirm the feature-route device-create chain and that the indirect-count path engages when fully supported.
- **LS-26 accepted perf trade, not yet benchmarked.** Contiguous-run folding raises the fallback encode count from <= 4 quartile buckets to the number of vertex-count transitions in the draw list (order preservation is required for equal-depth determinism). A paired MoltenVK benchmark run should quantify the delta on the city scene; the old numbers (2 encodes, 1.9 ms submit) are no longer representative.
- **LS-31 pipeline default-off.** The cluster_select compute pipeline is only built with `--gpu-selection`; nothing dispatches it, and creation failure is non-fatal. If/when GPU traversal lands, revisit the 11-SSBO layout against minimum descriptor limits.
- **LS-18 viewer port is corpus-verified, not contract-exhaustive.** Tags 11-13 parse and validate against every committed simval corpus (six contact streams bit-identical audio incl. static contactants, re-verified after the LS-33 contact-validation tightening, plus synthetic negative streams: out-of-range monopole id, a past body_count, wall id with walls off, real b past body_count), plus negative tests (unknown tag, misplaced/invalid ContactParams). Future stream-contract tags will need another port pass - the walker still rejects tags above 13 by design.
- **LS-19 perf trade-off is accepted, not benchmarked.** Removing the CPU normal-cone backface cull from the main pass costs draw-list entries and vertex work on cone-backfacing clusters; a paired benchmark run should quantify it. Cone/cull-sphere data remains in the cluster records for future one-sided material support.
- **Pass 1 is one scout pass, not an exhaustive audit.** Coverage: builder (cluster/serialize/traversal), streaming (scheduler/async reader/residency), HZB/occlusion, render/bootstrap, runtime model, shaders. Follow-up passes were planned (cumulative expansion protocol) but blocked by tooling; treat unlisted subsystems (e.g. trickplay/texturing internals beyond what pass 1 reached, tools/) as unaudited.
- **Known accepted behavior, documented in code:** LOD provenance merges (LS-01 fix) trade bounded over-emit for the whole-unit selection invariant; merged-record geometric_error is the max member error.
