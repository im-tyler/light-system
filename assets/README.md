# Sample Assets

This directory holds local sample assets used for builder verification and early benchmarks.

Current contents:

- `sample_rock.obj`: simple triangulated mesh used to verify the first-pass offline builder
